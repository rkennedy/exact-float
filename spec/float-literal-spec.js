"use strict";

/*
var exact = require("../src/exact-float.js").exact;

describe("A floating-point literal", function() {
    it("can be 32 bits", function() {
        expect(new exact.Float32("1.").to_hex()).toBe("0x3f800000");
        expect(new exact.Float32("0.").to_hex()).toBe("0x00000000");
        expect(new exact.Float32("-1.").to_hex()).toBe("0xbf800000");
        expect(new exact.Float32("-0.").to_hex()).toBe("0x80000000");
    });

    it("can be 64 bits", function() {
        expect(new exact.Float64("1.").to_hex()).toBe("0x3ff0000000000000");
        expect(new exact.Float64("0.").to_hex()).toBe("0x0000000000000000");
        expect(new exact.Float64("-1.").to_hex()).toBe("0xbff0000000000000");
        expect(new exact.Float64("-0.").to_hex()).toBe("0x8000000000000000");
    });

    it("can be 80 bits", function() {
        expect(new exact.Float80("1.").to_hex()).toBe("0x3fff8000000000000000");
        expect(new exact.Float80("0.").to_hex()).toBe("0x00000000000000000000");
        expect(new exact.Float80("-1.").to_hex()).toBe("0xbfff8000000000000000");
        expect(new exact.Float80("-0.").to_hex()).toBe("0x80000000000000000000");
    });
});
*/
